ctrl + alt + t      //Вызов терминала
sudo команда        //выполнение команды от имени администратора (root)
man команда         //вывод документации по команде

cd folder_name      //Вход в директорий
mkdir folder_name   //Создание директория
ls -l               //вывод содержимого директория с использованием широкого формата
                    //(размер файла, владелец файла, права доступа, дата изменения)
                    //Бывают параметры: 
                    //  -A — включить в список «скрытые» файлы («скрытые» файлы в Linux имеют в своем 
                    //       названии точку в начале имени файла, например, .gtkrc-2.0);
                    //  -S — отсортировать файлы по размеру;
                    //  -t — отсортировать по дате изменения;
                    //  -1 — выводить имена в столбик, по одному на каждой строке.
                    //Пример: ls -AS1
ll                  //то же самое, что и ls -l
dir                 //---//---
tree                //выводит дерево файлов и директориев
clear               //Очищает содержимое консоли

touch file_name     //создает файл
echo >> file_name   //создает файл

rm file_name        //удаляет файл
rm folder_name -r   //рекурсивно удаляет директорий и всё внутри него
cp file_name path/new_file_name     //копирует файл с возможностью изменения имени. Пример: cp file1.html ~/file2.html
cp -i file1.html ~/file2.html       //Перед перезаписью файла будет выдан запрос на ваше согласие, если он уже существует в папке.
cp -R file_name path/new_file_name  //копирует директорий рекурсивно из текущей папки в новое место возможно с переименованием

mv folder_name path/new_folder_name           //перемещает директорий
echo -e "строка информации" >> имя_файла      //Заносит текстовую информацию в файл

cat имя_файла       //Отображает содержимое файла
cat > имя_файла     //Создает новый файл и позволяет сразу вставить в него текст
nano имя_файла      //Открывает текстовый редактор для изменения файла.
                    //В редакторе:  ctrl + O - сохранить изменения;
                    //              ctrl + X - выйти из редактора
ls -l >> имя_файла  //направляет поток вывода в файл

gparted             //Программа редактор разделов
top                 //Программа системный монитор. Для выхода из неё q.
dmidecode           //Выводит системную информацию о ПК
lsusb               //Выводит список подключенных USB устройств
lspci               //Выводит список PCI устройств
lspci | grep ключевое_слово   //Выводит список PCI устройств с фильтрацией по ключевому слову

Права доступа к файлу: (-rw-rw-r--)
-|rw-      | rw-    | r--
  владелец | группа | остальные
chmod 777 имя_файла           //изменяет права на файл
chmod -R 777 folder_name/     //изменяет права на директорий и все, что в нём рекурсивно
chown www-data:www-data filename //изменяет владельца и группу соответственно у файла (https://habr.com/ru/post/469667/)

./ имя_файла                  //запускает файл из директория
mc                            //файловый менеджер. Удобно использовать с командой sudo
dpkg -i имя_пакета.deb        //установка пакета из файла источника .deb
wget http://адрес.deb         //закачка пакета по URL

nslookup адрес.хоста.ru       //посмотреть IP хоста
ifconfig                      //посмотреть IP адрес машины
netstat -plant |egrep '(8[0-9]{3}|2[0-9]{3})'   //посмотреть свободные порты
ss -ltnup 'sport = :8080'     //узнать PID процесса, занимающего порт 8080
kill -9 4041021               //принудительно небезопасно завершить процесс с PID 4041021
mc                            //Midnight Commander файловая оболочка. Выход - F10
whereis имя_файла             //Команда whereis работает примерно как и which, 
                              //однако она также ищет страницы руководства и файлы с исходными текстами.

scp ~/local_folder/file_name.txt  remote_some_host.ru:~    //копирование файла через SSH на удаленную машину в свою домашнюю папку там (Подробнее: https://www.shellhacks.com/ru/copy-files-ssh-10-examples/)

java --version                // узнать установленную версию Java
cat /etc/lsb-release          // узнать версию дистрибутива ОС
cat /proc/cpuinfo             // посмотреть информацию о процессоре

crontab -l                    // отображение содержимого crontab-файла для текущего пользователя
crontab -e                    // изменение расписания текущего пользователя
crontab -r                    // очистка расписания текущего пользователя
Задачи крона могут храниться в 8х местах:
1.     /etc/crontab (можно посмотреть глазами cat /etc/crontab)
2.     Клиентский crontab ( crontab -u user -l)
3.     /etc/anacrontab (можно посмотреть глазами cat /etc/anacrontab)
4.     /etc/cron.d/ (Кроме того, cron считывает файлы в каталоге /etc/cron.d/)
5.     /etc/cron.daily/       Запуск всех скриптов один раз в день
6.     /etc/cron.hourly/    Запуск всех скриптов один раз в час
7.     /etc/cron.monthly/ Запуск всех скриптов один раз в месяц
8.     /etc/cron.weekly/   Запуск всех скриптов один раз в неделю 
Для вывода всех задач можно пользоваться командой (https://www.linuxshop.ru/articles/a26710788-nastroyka_cron):

for user in $(cut -d':' -f1 /etc/passwd); do crontab -u $user -l; done

Наблюдение за работой процесса с идентификатором 10793390:
ps  -p $(pgrep  -f 10793390)  -o lstart,%mem,cmd        // процент от физической памяти которая выделена контейнеру
ps -A -f | grep '10793390'                              // краткая информация о процессе
cat /proc/$(pgrep -f 10793390)/status                   // подробная информация о процессе

// Управление кастомизацией маршрутов
ip route // просмотр всех маршрутов
ip route|grep via|grep default // получение информации о маршруте по дефолту. Возвращает, например, 192.168.0.1
sudo ip route add 1.2.3.0/24 via 192.168.0.1 // добавление маршрута для подсети 1.2.3.0/24
sudo ip route del 1.2.3.0/24 // удаление маршрута для подсети 1.2.3.0/24
sudo ip route add 1.2.3.4 dev vpn0 scope link // добавление маршрута для vpn соединения


// установка корневого сертификата .cer
/opt/cprocsp/bin/amd64/certmgr -inst -cert -file ~/nazvanie-sertifikata.cer -store uRoot

// удалить 30 строк в конце текстового файла (подходит для очень больших файлов, сотни мегабайт):
head -n -30 /tmp/log.mytable_dump.sql > /tmp/log.mytable_dump_result.sql
